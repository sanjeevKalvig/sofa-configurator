services:
  react-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: tushar638/configurator_demo3
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev -- --host
